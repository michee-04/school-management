export const DEFAULT_ROLE_SLUG = {
  USER: 'user',
  ADMIN: 'admin',
  SUPERADMIN: 'superadmin',
} as const;

export const DEFAULT_GROUP_SLUG = {
  USERS: 'users',
  ADMINS: 'admins',
  SUPERADMINS: 'superadmins',
} as const;
